# Changelog

All notable user-facing changes should be recorded here.

## Unreleased

- Self-signed TLS nodes work again from Xray clients (v2rayN, v2rayNG, Happ …), and every export now pins their certificate instead of skipping verification. Xray refuses `allowInsecure` outright since 2026-06-01 (v26.6.2 deleted the field) and current v2rayN no longer passes it on; it pins the certificate named in the link instead (`pcs`, Xray's share-link standard). PSM's links for such nodes carried only `allowInsecure=1`, so a VLESS, Trojan, VMess or Hysteria2 node that PSM signed itself (sing-box / mihomo without a domain) could not connect from any Xray-based client. The links now carry the certificate's SHA-256 next to the old flag — `pcs` in vless:// and trojan:// (also read by mihomo), `"pcs"` in vmess://, `pinSHA256` in hysteria2://, `hpkp` in anytls:// (mihomo) — so clients that know the pin check that one certificate and the others behave as before. `psm node export --format clash` adds `fingerprint` (mihomo checks it even with `skip-cert-verify` on, which stays for Stash), `--format singbox` writes `certificate_public_key_sha256` instead of `insecure` (sing-box 1.13+), and the menus' links and Clash snippets do the same. Measured with Xray v26.9.9, sing-box 1.14.2 and mihomo v1.19.31 as clients: every self-signed node connects pinned, a wrong pin is refused by all three, and the same link without `pcs` fails in Xray.
- Fixed Xray mKCP nodes that no client could connect to. Measured with real cores on both sides (Xray v25.12.8, v26.3.27 and v26.9.9 as client and server): the seed/header wire format is the same classic one in Xray v25's `kcpSettings.seed`, in v26.3.27's finalmask (`mkcp-aes128gcm` / `header-*`) and in v26.6's `mkcp-legacy` — which is what v2rayN 7.25 writes — but two things PSM did broke it. On Xray v26.9.9 (the preview channel) PSM preferred `kcpSettings.seed` because that core accepts it again, yet that form no longer interoperates with anything but itself: not with v2rayN, not with an older core. And before v26.6.22 Xray applied finalmask masks last to first, so on the stable v26.3.27 a node with a disguise header (srtp, utp, wechat-video) was written the wrong way round. PSM now asks the installed Xray which finalmask form it takes (`mkcp-legacy`, else `mkcp-aes128gcm`, else the old `kcpSettings` for an Xray without finalmask — v25 silently ignores the key, so a mask type that does not exist is tried first) and orders the masks for that release. `psm doctor` compares every mKCP inbound with what its node builds now and `psm doctor --fix` rewrites the ones an older PSM wrote.
- New Xray mKCP nodes get VLESS Encryption (X25519) by default. mKCP carries VLESS without TLS, and Xray clients from v26.7.7 on refuse VLESS without TLS or VLESS Encryption towards a public address ("vless without TLS or other encryption is prohibited"), so a plaintext mKCP node cannot be used from a current client. `--vless-enc none` (or answering no in the menu) keeps the old plaintext form for older clients.
- `psm node update … --vless-enc x25519|mlkem768|none` now does what it says: it makes the key pair, replaces it when the other kind is asked for, or removes it. Before, VLESS Encryption could only be chosen when a node was made; an update stored the option and changed nothing.
- Hysteria2: `--bbr-profile conservative|standard|aggressive` (and a question in the three add menus), the BBR congestion profile the server sends with — `bbr_profile` in sing-box 1.14, `bbr-profile` in mihomo 1.19.24, `finalmask.quicParams.bbrProfile` in Xray v26.4.13. Unset keeps the core's default (standard). An older core would refuse the field (sing-box) or ignore it without a word (Xray, mihomo), so PSM asks for the version that has it; Xray's stable release (v26.3.27) does not, its preview channel does.
- The Xray VPNGate exit writes its IPv4-only resolution as `sockopt.domainStrategy`: Xray v26.9.9 deprecates `freedom.domainStrategy` ("will be removed"). Both Xray releases PSM installs accept the new form without warnings.
- `psm node export --format clash` now covers Xray's XHTTP nodes too (XHTTP, WebSocket, gRPC, HTTPUpgrade as mihomo's `v2ray-http-upgrade`, HTTP/2 as XHTTP stream-one, and the REALITY layer), so the panel's Clash subscription can carry them as mihomo proxies rather than share links. mKCP has none (mihomo's VLESS has no mKCP).
- Fixed obfuscated Hysteria2 nodes not connecting from PSM's sing-box export: the outbound had no `obfs`, so the server dropped every packet. Nodes with VLESS Encryption are left out of the sing-box export (sing-box has no VLESS Encryption) instead of producing an outbound that cannot connect.
- Core versions: the offline fallbacks are sing-box v1.14.2 and mihomo v1.19.31. Every generated configuration is checked against Xray v26.3.27 and v26.9.9, sing-box 1.14.2 and 1.15.0-alpha.8 (the preview channel) and mihomo v1.19.31; `tests/core-validate.sh` can print what a core warns about (`SHOW_WARNINGS=1`), which is how the VPNGate deprecation above was found.

- Removing a server from the panel now takes its relays off the machine as well. The leaving plan listed the nodes and the standalone servers, so `psm agent remove` left realm's rules — and the accounting rules that go with them — behind on a machine the panel had forgotten. The plan carries the relays too, and psm-agent deletes each one; an agent too old to know the field ignores it and behaves as before. psm-agent 0.9.0.

- Fixed `psm relay probe` taking three seconds for every sample. It measured the connect with `curl telnet://`, which connects at once but then sits there until `--max-time` expires: ten samples cost 31 seconds, so a server with a couple of relays spent half of every minute probing. bash opens the connection itself now (`/dev/tcp`) and the shell exits as soon as it is up — the same measurement in 0.36 seconds. `timeout` still bounds a port that silently drops packets, and spawning that subshell puts a floor of roughly 5 ms under the reading, which matters only for a hop to 127.0.0.1.
- Fixed the panel's relay readings arriving every 90 seconds rather than every 60. A measurement rides the next sync, and the probe itself took 31 seconds (above), so the 60-second timer was always missed by the sync that should have carried it. With the probe fast again the readings land 60 seconds apart; psm-agent also shortens its wait when the next measurement is due sooner than the panel's idle interval, so the interval holds whatever that is set to.
- psm-agent passes `--mount-443` when the panel asks for a node on the shared 443 (`mount443` in the task). Sharing the port is not a node setting but how the node is created, and PSM refuses to move one onto or off it as an update, so the panel fixes it at creation and never sends it again.
- `psm relay probe [TAG] [--samples N] [--json]`: how a hop is doing — round trip, jitter, loss and the bytes the relay has carried. The round trip is measured with plain TCP connects to the landing side rather than with ping, because ICMP is filtered often enough on these networks that ping would report loss that is not there, and a relay carries TCP anyway, so a connect is what the traffic actually experiences. Jitter is the mean absolute difference between consecutive round trips over the burst. A relay whose landing side never answers reports `rtt_ms: null` and 100% loss instead of a made-up number. psm-agent runs this every 60 seconds and sends the readings with its sync, the way it already sends the traffic counters; the panel keeps 7 days of them for its charts.
- A relay is metered on the same `PSM_TRF` accounting chain the nodes use, by its listening port, under the tag `relay-<tag>` so it can never collide with a node's. The rules are put in place when the rule is applied and taken away when it is deleted — including the old port's rules when a relay's listening port changes, which would otherwise keep counting for a port it no longer listens on.
- Fixed a relay that cannot listen being reported as running. realm keeps going when a single endpoint fails to bind — it logs `[tcp]failed to bind 0.0.0.0:PORT: Address in use (os error 98)` and serves the others — so the service stays active, and PSM, which judged the result by exactly that, wrote the rule and told the panel it was applied while the relay could not accept a single connection. A port that no rule already uses is now checked for another listener before the rule is applied, and the port is checked again afterwards; either way the previous rules are put back and nothing changes. The same class of bug as the sing-box one below, except that here the service does not die at all, only one of its endpoints does.
- `psm relay add|update` take their settings as JSON as well (`--input -`, `--input FILE`, `--data '{…}'`), the way `psm node add` already did, so psm-agent can send a relay exactly as it sends a node; a command-line flag still wins over the same field in the JSON. psm-agent gained the `relay.add`, `relay.update`, `relay.delete` and `relay.list` tasks, checked by their own validator — a relay has no core and no protocol, so the node check does not apply to it — and installs realm on first use, the way it installs a missing core for a node.
- Fixed `psm relay update` never being able to turn UDP or the TLS hop **off** through `--input`/`--data`: jq's `//` stands in for `false` as well as for null, so `{"udp": false}` read as "this field was not given" and left the switch as it was, while `true` worked. A boolean is now read by whether its key is present at all. This is the path the panel uses to change a relay, so from the panel a switch could only ever be turned on; `psm relay add` was not affected, because there a missing field and `false` mean the same thing.
- `psm relay list|show|add|update|delete|install`: the realm relays without questions, so the PSM panel can manage them (the panel's 中转). The entry machine forwards a port to the landing machine, whose nodes do not change — realm works at L4 and neither parses nor decrypts what passes through. Only the entry machine needs a rule. The rules stay in `config/realm/rules.json`, the menu's own store, and realm's `config.toml` is generated from it as before, so a relay made from the menu and one made from the command line are the same thing.
- A relay's hop can be encrypted: `--tls` wraps the forwarded stream in TLS, so what travels between the two machines no longer looks like the node's own protocol. Which side a rule is follows from where it forwards: to this machine (127.0.0.1) it terminates TLS and holds the certificate (`--tls-cert`/`--tls-key`, else a self-signed pair made for `--tls-sni`), to another host it dials it (`--tls-sni`, with `--tls-insecure` to accept a self-signed one). Encryption is off by default and the menu's plain rules are untouched. realm writes this as `listen_transport` / `remote_transport`, measured on realm 2.9.4, which reports `transport=[tls][plain]` for such a rule.
- `realm_install_unattended` installs realm without questions, the way `psm core install --if-missing` installs a core: `psm relay add` and `psm relay install` fetch it when it is missing, so a server that never ran a relay needs no menu visit first.
- Fixed the Xray REALITY node the menus could not add: `reality.sh: line 374: server_names_raw: unbound variable`, and the script ended there (#4). The own-domain question defaults to no, and answering it leaves `server_names_raw` unassigned — `local x` declares a variable without setting it, so the camouflage branch that reads it next ends the script under `set -u`. It is initialised empty now. This came in with `fbfbeef`, which replaced the hardcoded public camouflage domain with "discover or type one", but kept the declaration as it was; the command line (`psm node add xray reality …`) was never affected, which is why the suites, all of which use it, did not catch it.
- `psm doctor` reports whether `/dev/net/tun` is there, with the kind of virtualisation the server runs under. Containers — LXC and OpenVZ especially — usually have no TUN device unless the host hands one out, and the free residential exit (VPNGate, over OpenVPN) cannot work without it; WARP can, since all three cores speak WireGuard themselves. The check is a warning, not a failure, and says to ask the host for TUN or to use the WARP exit instead. psm-agent already reports `psm doctor --json`, so the panel's diagnostics show it too, and the panel's exit field now says the same before a node is made.
- The menus can edit a node now: **修改节点（端口 / 密钥 / 域名）** / "Edit a node (port, keys, domain)", one entry in each core's menu (sing-box and mihomo item 15, Xray item 14). It lists that core's nodes whatever their protocol and changes one field — port, password, UUID, SNI, domain, REALITY's serverName, dest or short id, or the public port — through `_node_cli_cmd_update`, the same path the command line and the panel use. The protocols' own menus only ever offered a subset: AnyTLS could change its password but not its port, and Hysteria2, TUIC, VLESS and WireGuard on sing-box and mihomo offered nothing at all, so a node made in the menus could not be edited there. Pressing Enter leaves the field alone (an empty value would have written an empty password).
- Fixed a node with an exit registering WARP, or dialling the residential line, on a server whose core is not installed — and then failing with "sing-box did not take the WARP outbound", which named the wrong cause. `psm node add|update` checks that the core is there before it prepares the exit, so nothing is left behind for a node that cannot be made and the error says what is actually missing. Reported as "the panel cannot install AnyTLS with a WARP or residential exit"; on the panel's own path psm-agent installs the core first, so this hit the command line and any path where the core was missing.
- Fixed `psm node update … --server-name` leaving REALITY's camouflage domain unchanged in the core. The node store keeps `server_names_raw` (the raw comma-separated list) beside `server_name`, and Xray's inbound is written from `server_names_raw // server_name`; only the latter was updated, so the core kept serving the old domain while the node and its share link showed the new one. The raw list now follows a changed `server_name`, unless `--server-names-raw` is given (several SNIs).
- `psm --update <what>`: `scripts`, `xray`, `singbox`, `mihomo`, `hysteria2`, `nginx`, `geo` or `all`, without questions. `psm --update` alone still opens the menu; before, that was the only form, so psm-agent, the panel or cron calling it got the menu printed at a stdin that is not there, and a non-zero exit instead of an update.
- The menus can set a node's exit now: **节点出口分流（WARP / 家宽）** / "Per-node exit (WARP / residential)", one entry in each core's menu (sing-box and mihomo item 14, Xray item 13). It lists that core's nodes whatever their protocol, with the exit each one has, and sets or clears it — writing the same rule as `psm node add … --exit`, so a node set up from the menu, from the command line or from the panel is the same thing. Reported as "AnyTLS cannot use the residential exit": the command line and the panel could always do this for every protocol (measured here on sing-box, mihomo and Xray, with WARP and VPNGate), but no menu ever asked. The routing menu's own WARP and VPNGate entries write geosite rules for the whole server, not for one node, so from the menus there was no way to give a single node an exit. Nothing about it was specific to AnyTLS.
- Remote rule sets are written the one way sing-box 1.14 accepts without a deprecation: `http_client: {detour: ""}`. Measured by starting 1.14.1 on each form: `detour: "direct"` fails outright ("detour to an empty direct outbound makes no sense"), leaving the field out — or writing `http_client: {}` — warns that the implicit default HTTP client "will be removed in sing-box 1.16.0", and the legacy `download_detour` warns the same. Older sing-box keeps `download_detour`. PSM fetches rule sets to local files anyway; this covers the fallback when that download fails.
- Fixed sing-box 1.14 refusing to start — every node on the server down — as soon as a routing rule used a geosite or geoip set. PSM wrote each remote rule set with `http_client: {detour: "direct"}`, and 1.14 rejects a detour to its plain direct outbound ("detour to an empty direct outbound makes no sense"); the field is simply left out now (the default HTTP client is direct anyway). It hit the routing menu's geosite rules, its "unlock Netflix / OpenAI through WARP" preset and the new per-node exits alike.
- Rule sets are fetched by PSM when the rules are applied and referenced as local files (refreshed daily), instead of being downloaded by sing-box while it starts. A download that fails during startup takes the whole core with it — on a server that cannot reach GitHub, for good — so the core no longer depends on the network to come up. If the fetch fails, the remote rule set is written as before and sing-box tries again itself.
- Fixed a config sing-box cannot run staying in place: `sing-box check` only reads the config, and `systemctl restart` reports success even when the unit dies straight after and enters its restart loop, so PSM's rollback never fired (it had already dropped the backup). A change is now kept only if the service comes up and stays up; otherwise the previous config is restored, restarted, and the service log is shown.
- Fixed `psm node add` opening no firewall port on servers with an iptables firewall (and, by chance, with ufw). The check for an enforcing firewall piped `iptables -S` into `grep -q`; grep stops at the first match, which is the policy line, iptables could die of SIGPIPE while still writing, and under manager.sh's `set -o pipefail` the match then counted as a failure: no firewall found, no port opened. The firewall checks read the output first now, and an iptables rule that did not go in is reported instead of being taken as done. Found by the PSM panel's every-node test on Alpine.
- Fixed the sing-box subscription of a set of nodes that includes mihomo's VLESS over XHTTP: sing-box has no XHTTP transport, its outbound was written anyway, and sing-box refused the whole config. Such a node has no sing-box outbound now (it stays in the share links and the Clash profile).
- Exits for one node's traffic: `psm node add|update … --exit warp|vpngate [--exit-sites all|ai|streaming|GEOSITE,…] [--exit-country CC]` routes what comes in on that node — all of it, or only those sites (ai: OpenAI, Anthropic, Gemini; streaming: Netflix, Disney+, HBO, Prime Video, Spotify) — through Cloudflare WARP or the free residential line (VPNGate), in Xray, sing-box and mihomo alike; everything else goes out directly as before. The rule is tied to the node (`.node` in the core's routing store), so updating the node rewrites it, `--exit none` or deleting the node removes it. WARP is registered on first use; the residential tunnel is connected on first use (a residential or mobile line in CC, default JP; one per server, kept while it is up, with its watchdog on). The exit is set up before the node is made, so a line that does not answer leaves nothing half made. `psm exit status|warp|vpngate` shows and prepares them. For this, an inbound routing rule can carry `geosite` (Xray: inboundTag with domains; sing-box: inbound with rule sets; mihomo: `AND,((IN-NAME,…),(OR,(GEOSITE,…)))`).
- `psm sni find [--engine netlas|quake|zoomeye|fofa] [--key-stdin] [--json]`: the REALITY menu's search for camouflage targets in the server's own network, without questions — a cyberspace-mapping engine lists hosts in the same ASN with their certificate names, each is checked with a TLS handshake, the healthy ones come back by latency. The engine's key can come on stdin for that one search (it is not saved); without it the menu's saved key is used.
- psm-agent 0.5.0: the `sni.find` task (the engine's key travels on stdin only), and node changes run with the long timeout, since a node whose exit is the residential line connects it first. `lib/agent.sh` installs 0.5.0.
- Nodes made with `psm node add` (and so every node from the PSM panel) now work on a fresh server. A user found that on the panel many sing-box protocols would not install, and the ones that did could not be reached. Three causes, all fixed:
  - The TLS protocols of sing-box and mihomo (Hysteria2, TUIC, AnyTLS, VLESS + TLS, Trojan, VMess) and Xray's Hysteria2 required `cert_path` / `key_path`, so on a server without certificate files they were refused. Without them now, a node takes its SNI's certificate when PSM has one (`/etc/nginx/ssl/<domain>/`), else a self-signed one (SNI `www.bing.com` unless given), with `insecure = 1`, as the menus do. Certificate paths that do not exist are refused with that reason.
  - `psm node add` never opened the node's port; only the menus did. It opens it now in the enforcing firewall (ufw, firewalld or an iptables default-deny: UDP for Hysteria2 / TUIC / WireGuard / mKCP / QUIC, TCP and UDP for SS2022 / Snell / SOCKS). A rule PSM added is recorded (`config/firewall-ports`) and removed with the last node on that port, and on a port change; a port the firewall already allowed is left alone.
  - Self-signed VLESS and Trojan links lacked `allowInsecure=1`, the sing-box outbounds of VLESS / Trojan / VMess lacked `tls.insecure`, and mihomo's share-link import cannot skip verification for VLESS, VMess or TUIC at all. The links and outbounds carry it now, and `psm node export … --format clash` prints the TLS protocols as ready mihomo proxies (with `skip-cert-verify`), which psm-agent 0.4.1 sends to the panel for its Clash subscription.
  - Xray's domain protocols (Vision, XHTTP over TLS, Trojan, VMess) for a domain without a certificate are refused before anything changes, saying how to get one; before, Xray refused its whole config and every Xray node on the server went down.
  Covered by `tests/integration/panelside.sh` (behind an iptables default-deny: self-signed nodes made, their ports opened and shut again, a sing-box client through a self-signed Hysteria2 node) and by the panel's every-node test, which now runs each server behind ufw, firewalld or iptables with no certificates and connects from a separate client machine.
- Fixed deleting the last Xray SS2022 node leaving it running: PSM removed it from its list, but the rebuild returned early for an empty list, so the inbound stayed in Xray's live config and its port kept listening (deleting one of several was fine). Found by the PSM panel's every-node test (the panel deletes nodes through `psm node delete`); `tests/integration/panelside.sh` now checks that deleting one of two SS2022 nodes and then the last closes both ports and leaves no shadowsocks inbound.
- psm-agent 0.4.1: removing a server from the PSM panel now cleans up the server. The panel sends `agent.leave` with the nodes and standalone servers it made there; psm-agent deletes them (nodes made on the server's own command line are not in the list and stay, as do PSM and the cores), reports what it removed, and once the panel has that report runs `psm agent remove --yes` outside its own service (a transient systemd unit, or a new session under OpenRC, so stopping psm-agent does not stop the removal). The plan is checked entry by entry before anything is removed. `psm agent join` now installs 0.4.1.
- Fixed `bash <(curl …) --panel … --join …` stopping with `PSM_DIR�: unbound variable` on a server that already had PSM, and ignoring `--panel` / `--join`. In `"…（$PSM_DIR）"` bash read the first byte of the full-width bracket as part of the variable name under some locales; such variables are braced now, and `scripts/ci.sh` fails on any `$NAME` written directly before non-ASCII text. bootstrap.sh now takes `--panel URL --join TOKEN` (the PSM panel's install command): on a fresh server it installs PSM without questions (install.sh with `PSM_UNATTENDED=1`: no language prompt, no menu at the end) and then connects the server to the panel; on a server with PSM it updates PSM and connects it. The interactive install and update are unchanged.
- New scriptable commands, used by the PSM panel's psm-agent and usable on their own. The menus are unchanged:
  - `psm core list|install xray|sing-box|mihomo [--if-missing] [--json]` installs a core without questions (the latest stable release and its service), so a server that has only PSM gets the core its first node needs.
  - `psm standalone install|show|export|remove snell|ss2022` installs the standalone Snell server (v4, v5 or v6: the newest official build of that major version; v6 is still a beta upstream, so its newest beta or release candidate) or ss-rust without questions, with a given port and PSK / key / method, on systemd and OpenRC, into the config files the menus use. `export` gives the Surge line, the `ss://` link or a sing-box outbound. The official snell-server does not run on musl, so on Alpine it is refused with that reason (Snell runs there on sing-box or mihomo).
  - `psm traffic list|set|reset|unset [--json]` meters a node (a core node by its tag, the standalone servers as `snell` / `ss2022`) and limits it: `set TAG --limit-bytes N | --limit-gb N [--reset-day D]`, where a limit of 0 meters without limiting (the traffic menu only metered limited nodes). A node over its limit is paused by the periodic check until the monthly reset or `psm traffic reset`.
  - `psm node export … --format singbox` prints the node as a sing-box client outbound.
  - `psm agent join --panel URL --token TOKEN` downloads psm-agent from this repository's release (checked against its SHA256SUMS), joins the panel and runs it as a service; `psm agent status`, `psm agent remove`.
  - `psm version` prints the date and commit of the checkout.
  Covered by `tests/integration/panelside.sh` on Debian, Alpine and Rocky Linux 9: PSM's unattended install, a core installed on demand, standalone Snell v4/v5/v6 and ss-rust installed, replaced on a new port, exported and removed (a sing-box client reaches the internet through ss-rust), and traffic counted, limited (the node refuses connections), reset and unset.
- psm-agent 0.4.0: installs a node's core first when the server has never run it, installs and removes the standalone Snell / ss-rust servers, sets and resets traffic limits, sends the traffic counters to the panel every ten minutes (or when the panel asks), exports each node's sing-box outbound for subscriptions, and gathers a status report (PSM's version, the cores, `psm doctor`, the nodes, the traffic, the standalone servers) for the panel's diagnostics. New task arguments are checked against allowlists like the rest (a PSK or key that could be read as an option never reaches psm). Released as GitHub release `agent-v0.4.0` by `.github/workflows/agent-release.yml` (amd64, arm64, armv7 and SHA256SUMS; the tag must match the version in `agent/main.go` and `lib/agent.sh`).
- ss-rust's download needs `xz`, which Debian and Ubuntu package as `xz-utils`: the name is mapped now, so the native ss-rust install works there too (it was only used on OpenRC before).
- Downloads retry. A single 500 or timeout from GitHub while fetching a core used to fail the whole install (it happened in CI: sing-box's release download answered 500 once). Every download of a core (Xray, sing-box, mihomo, Hysteria2, realm, Snell, ss-rust, cloudflared), of jq, the acme.sh and Docker installers, rule sets, the geo data, the VPN Gate server list and the latest-version lookups now retries five times on timeouts and HTTP 408/429/5xx, backing off 1, 2, 4, 8 and 16 seconds (a first version waited a fixed 2 s three times, which a 504 from GitHub lasting longer outran in CI); bootstrap.sh does the same for its own download. Covered by `tests/integration/retry.sh` on Debian, Alpine and AlmaLinux 8: against a local stand-in for GitHub that refuses every file twice before redirecting to the real one, the sing-box and Xray installs still succeed, a lasting error still fails after six tries, and no download line in the scripts is left without the retries.
- Added `agent/`: psm-agent, which connects a server to the upcoming PSM panel (jinqians/psm-panel). A small static Go program with no dependencies that opens no port at all: like Xboard's node backends, it connects out to the panel over HTTPS (every 30 seconds when idle, every 3 while there is work; the panel sets the pace), reports the results of its last tasks and takes new ones. `psm-agent join -panel … -token …` trades the one-time token from the panel's install command for the server's own agent token (kept in a root-only config); `psm-agent run` is the sync loop. Every task is checked against allowlists (core, protocol, a node name that cannot start with "-", settings size) and run as a psm `--json` command with its arguments passed as an argv array, never through a shell; a task that fails the checks never reaches psm. Results survive a failed sync and are delivered on the next one. PSM does not install it yet: the install command (`bootstrap.sh --panel … --join …`) comes next. CI vets and tests it and builds it for amd64 and arm64. It replaces the loopback HTTP psm-api of the previous commit: the panel no longer needs a subdomain or a Tunnel per server.
- Fixed the main menu's right-hand column drifting left on servers without a UTF-8 locale (a fresh VPS often has none, and an SSH client's LANG may not exist on the server). The padding counted `${#s}`, which is bytes under the C locale, so every Chinese or Korean label looked three cells wide instead of two; Russian labels were off in every locale (counted 1.5 cells a letter), and the longest one overflowed the fixed 20-cell column. Label width is now read from the UTF-8 bytes whatever the locale (one cell for ASCII and two-byte sequences, two for three- and four-byte ones), and the left column is as wide as its longest label. Checked in all four languages under both the C and the C.UTF-8 locale; the full suites check that the menu is byte-identical in both.
- Installing and updating no longer download the READMEs, the screenshots, the CI files or the tests to the server; `/opt/psm` holds only what PSM runs. bootstrap.sh clones with a partial-clone filter and a sparse checkout, so those files' contents never leave GitHub, and all three update paths (running the install command again, the menu's auto-update, `update.sh`) turn an existing full clone into the same slim one before they pull, removing the files it had. Their pulls skip the diffstat (`--no-stat`), which would otherwise read, and so download, every changed README and screenshot. Covered by `tests/integration/slim.sh` against a local stand-in for GitHub on Debian 13, Debian 10 (git 2.20), Ubuntu, Alpine and the Red Hat family: after each path the files are absent, their contents are not in git's object store, and the checkout is clean.
- The main menu shows the PSM logo (the documentation site's "P" with a dot) beside the title, drawn with box characters in the logo's green and blue (256-colour terminals; basic green and blue elsewhere). A terminal narrower than 66 columns gets the title alone, so the menu never wraps. The README banner now carries the logo and every protocol PSM serves.
- Ubuntu 24.04 / 22.04 and the Red Hat family (Rocky Linux 9, AlmaLinux 8) are now tested, next to Debian 13 and Alpine 3.22. Ubuntu passed every suite as it was (22.04 ships jq 1.6 and relies on the jq fix below); the Red Hat family's first runs found these, all fixed:
  - jq 1.6, the EL8 / EL9 package, exits 0 for `jq -e` on empty input (jq 1.7 exits 4), and PSM branches on `jq -e` in many places whose input can be empty: a missing WARP outbound counted as present, a rule kind as bound, node_cli's store checks as passed. Where the installed jq is older than 1.7, PSM installs the official static jq 1.8.1 into `/usr/local/bin` (checked against the release's `sha256sum.txt`) and puts that directory first on PATH, so cron jobs and services use it too. It happens at install and on migrate import; `psm doctor` reports the jq version and `--fix` upgrades it.
  - The latest release of each core was looked up through api.github.com, which allows 60 requests an hour per IP without a token. A few installs behind one address use that up, and every install then fell back to a pinned version; for sing-box that was 1.13.19, which cannot run the Snell and gecko nodes PSM creates. The `/releases/latest` redirect is now asked first (it is not rate limited), then the API, then the pinned version (now v1.14.0 for sing-box), for Xray, sing-box, mihomo, Hysteria2, realm and ss-rust.
  - systemd older than 231 (Amazon Linux 2 ships 219) cannot start a core unprivileged, so there the cores stay root with all of root's capabilities, PSM no longer hands their files to `psm-core`, and `psm doctor` says why.
  - EL8's default Nginx (module stream 1.14) is built without the stream `ssl_preread` module, so the shared 443 could not start. On EL8 PSM enables the newest Nginx module stream (1.24) before installing, and moves an existing 1.14 install over.
  - `psm user` compared files with `cmp`, which minimal RHEL installs lack; it uses `cksum` now.
  - `psm user list` and the Cloudflare DNS record list printed nothing on systems without `column` (a minimal Debian has no bsdextrautils); both format their own tables now. Found by the documentation screenshots.
  - The integration tests decided "node created" with the same `jq -e`, so on jq 1.6 every refused node counted as created; they now match the JSON text.
  SELinux cannot be exercised in containers and is untested.
- Added accounts on the nodes, for all three cores: `psm user add NAME [--nodes all|TAG,…] [--days N | --expires DATE] [--quota SIZE]`, with `list`, `show`, `update` (nodes, expiry, quota, `--reset-usage`, `--enable` / `--disable`), `delete`, `links` and `token`. Each user gets one UUID and one password, used on every node assigned to them (VLESS / VMess / TUIC take the UUID; Trojan / Hysteria2 / AnyTLS / TUIC / SOCKS the password), and their own subscription URL under the online subscription's domain, which `token` replaces (the old URL stops working at once). The node's own credential stays as it is, so existing links keep working. Accounts are merged into each core's config on every restart PSM does, just before the core checks it; entries PSM added before are recognised and replaced, so applying is idempotent. Disabled, expired and over-quota accounts are left out, and the minute-by-minute traffic check re-applies when one of them changes state. Xray counts traffic per user, so a quota counts a user's traffic through Xray nodes per calendar month and pauses the account everywhere when reached; sing-box and mihomo expose no per-user counters and keep node-level quotas. Shadowsocks 2022, Snell and WireGuard stay single-key (users on an SS2022 server would change its owner's link). Accounts move with `psm migrate`. Verified on Debian 13 and Alpine 3.22 with 18 nodes across the three cores (`tests/integration/users.sh`): through a mihomo client, every user's links get through each node they were given and are refused on the others, the owners' links keep working, a user's traffic shows up in Xray's per-user counters, and a quota, an expiry date (set directly and reached between two periodic checks), disable / enable and delete each remove or restore the account on all three cores.
- Added `psm migrate` for moving a server to another host. `export` writes one bundle (optionally encrypted with a passphrase) holding PSM's state, the three cores' configs, every certificate and the acme.sh account, Nginx's shared-443 routing and camouflage sites, and the standalone services' configs, with checksums. `import` rebuilds it: it installs what PSM needs (the GNU userland on Alpine), the same core versions for the new system (musl builds on Alpine), service definitions for its init system, restores the files, runs the cores as `psm-core`, opens the nodes' ports, reinstalls Nginx, points acme.sh's renewal cron and reload hooks at the new host, rewrites online subscriptions for the new address under the same URL, restores port hopping and the scheduled jobs, and finishes with `psm doctor`. `push root@HOST` does all of it in one step over SSH and copies PSM itself, so the new server needs nothing but SSH (bash is installed first on a bare Alpine; the files are streamed over ssh because a minimal Alpine has no SFTP server for scp). Keys, UUIDs and passwords travel unchanged: nodes reached by domain keep working once DNS points at the new server, and IP-based links carry the new address. Standalone Hysteria2 / ss-rust / Snell / realm get their configs back but are not reinstalled. A server that already has nodes is only replaced with `--force`, after a backup. Also in the backup menu. Verified with two containers each way (Debian 13 → Alpine 3.22 by `push`, Alpine → Debian with an encrypted file and a wrong-passphrase refusal): byte-identical core configs and share links on the new host, the online subscription served at the same URL, and a client reaching the internet through every node there.
- Fixed online subscriptions being unreachable when Nginx owns 443: `sub_online_enable` set up the camouflage site but never added the subscription domain to the SNI map, whose default drops unknown names, so the TLS handshake was cut unless another node happened to route that domain. The domain now gets a route to the site (a domain that already routes to a node keeps it), and disabling the subscription removes it again. Found by the migration test.
- `psm doctor --fix` repairs what the checks find, then checks everything again (with `--json`, the report also lists each repair and its result). It restarts a stopped core through its config-test path, writing a missing service definition first; moves a core that still runs as root to `psm-core`; re-enables a core that no longer starts at boot; rebuilds lost Hysteria2 port-hopping rules; installs missing curl / jq / openssl; asks acme.sh to renew an expiring certificate; and gives disk space back (journal, oversized PSM logs, package cache). An invalid config is only ever reported, never rewritten. Doctor also gained the boot, run-as-user and port-hopping checks, and a plain `psm doctor` says how many problems `--fix` can repair. `--fix` needs root; without it doctor stays read-only.
- Xray, sing-box and mihomo no longer run as root: they run as the system user `psm-core`, keeping only the two capabilities they need (binding ports below 1024, and the routing mark the WARP / VPNGate exits use), on systemd and on OpenRC alike. Before every start, and after every config PSM writes, a root step (`lib/coreperm.sh`) makes the configs, the core's own state (mihomo's home and providers, sing-box's cache and rule sets, Xray's logs) and every certificate the config references readable by that user. Keys PSM installs are `0640 root:psm-core`, and acme.sh renewals, which rewrite the key, keep them that way. A key outside PSM's certificate directories is reported instead of being opened up. Existing installs switch over on the next change or core restart made through PSM.
- Added TUIC v5 inbounds for sing-box and mihomo (QUIC over UDP, uuid + password, bbr / cubic / new_reno congestion control, a real domain certificate or a self-signed one). Nodes come with a `tuic://` share link, a Clash snippet and a QR code, and `psm node add … tuic` works for both cores; 0-RTT stays off because it can be replayed. Verified end to end: a mihomo client that imports PSM's link reaches the internet through both servers.
- Added Hysteria2 port hopping on sing-box, mihomo and Xray nodes (`--hop-ports START-END`, or one question in the add flow). A UDP port range is redirected to the node's port for IPv4 and IPv6; the firewall only needs the node's own port. The rules are rebuilt from the node stores after every change and at boot (a systemd oneshot, or `/etc/local.d` on OpenRC), because Debian does not restore iptables rules by itself; minimal Debian 13 has no iptables at all, so it is installed when hopping is first used. A range is refused when it would swallow another node's port or another UDP listener. Share links use the Hysteria2 multi-port form (`host:443,20000-21000`), and the sing-box client export gets `server_ports`. Verified from a client in its own network namespace: a client that only ever uses the hop range (never the node's port) gets through.
- Added a WireGuard server on sing-box (an `endpoints` entry): its own /24, one or more client peers, and a standard wg-quick file per peer with a QR code (`psm node export sing-box wireguard TAG`). Verified with two peers connecting from WireGuard clients built from those files. mihomo has no WireGuard listener, and AmneziaWG is left out: none of the three cores can act as an AmneziaWG server.
- Added ECH (Encrypted Client Hello) as a toggle on sing-box and mihomo VLESS, Trojan, AnyTLS, Hysteria2 and TUIC nodes: `psm node update … --ech true|false`, or the new ECH entry in the sing-box / mihomo menu. Keys come from whichever core is installed. Measured with the real cores: an ECH server still accepts clients that do not use ECH, so turning it on never breaks existing share links, and it also works over QUIC. Share links cannot carry the ECH config; PSM's sing-box client export includes it, and `psm node export … --format ech` prints it for mihomo's `ech-opts`. Verified end to end, including a client holding another server's ECH config being rejected.
- The tests are now public and run on every push to `main`: `tests/` and `scripts/ci.sh` are no longer ignored, and `.github/workflows/ci.yml` runs them on the project's own test VPS through a self-hosted runner, each job in a disposable container. The workflow has no `pull_request` trigger, so code from a fork never runs on that machine. The checks are syntax, ShellCheck, i18n and the config snapshots; every generated config against the real cores (Xray stable + preview, sing-box, mihomo); and integration suites on Debian 13 (systemd) and Alpine 3.22 (OpenRC): a full install-to-uninstall matrix, the Nginx 443 SNI path with client traffic through it, standalone Snell on Alpine, and an end-to-end suite in which a mihomo client imports the subscription PSM exports and must reach the internet through every node. `tests/integration/container.sh debian|alpine SUITE` runs any suite by hand the same way.
- Fixed Xray XHTTP (HTTPUpgrade mode) and sing-box VLESS HTTPUpgrade nodes not working in PSM's mihomo client export, found by the new end-to-end suite. The share link is correct (Xray and a hand-written mihomo proxy both connect with it), but mihomo's link importer turns `type=httpupgrade` into a network its VLESS client does not know unless the path carries `?ed=`, so the imported node never sends the upgrade (mihomo v1.19.30). The export now lists these nodes explicitly as `ws` + `v2ray-http-upgrade` and keeps them out of the subscription provider. Importing the raw link into a mihomo client by hand is still affected until mihomo fixes its importer.
- REALITY camouflage targets are now tested with a real handshake through the node's own core before a node is created: a throwaway server and client of that core on loopback must carry one request to the target. The TLS checks alone are not enough: measured, `www.microsoft.com` passes all of them, yet an Xray REALITY node pointed at it never completes a handshake. The interactive flows treat a failed probe like a failed check (pick another target or proceed anyway); `psm node` refuses the target unless `--skip-dest-probe` is given. A machine where the probe cannot run (core not installed, no curl) is not blocked.
- `psm node` can put nodes on the shared 443: `add --mount-443` listens on loopback and routes the node's SNI through Nginx (installed when missing), refusing an SNI that already routes elsewhere. `update` carries port and SNI changes into the SNI map, `delete` removes the node's route, and a route Nginx rejects rolls the node change back. Until now nodes behind Nginx could only be created and changed from the menu.
- Fixed mounting a node on the shared 443 (Nginx SNI routing) on Alpine: Nginx refused to start with `"stream" directive is not allowed here in /etc/nginx/conf.d/stream.conf`. Alpine's `nginx-mod-stream` ships a top-level `stream {}` in `conf.d`, and PSM's `nginx.conf` includes `conf.d` inside `http {}`. PSM's own `stream {}` already covers it, so the distro file is kept as `.psm-disabled` and blanked; a package upgrade puts its new copy in `.apk-new`, which is not loaded. PSM's `nginx.conf` also used Debian's pid path everywhere, so on Alpine `rc-service nginx` could not see or stop the Nginx it had started; the pid path now comes from the Nginx build. Verified on Alpine 3.22 and Debian 13 with real client traffic through 443 to Xray, sing-box and mihomo REALITY nodes.
- Standalone Snell now installs on Alpine: the official snell-server cannot run on musl, so PSM runs the upstream `jinqians/snell-server` image (it carries its own glibc runtime) under an OpenRC service, installing Docker first if needed. It uses the same `snell-main.conf`, host networking and service name as the native install, so status, restart, logs, the firewall rule, traffic accounting, update (`docker pull`) and uninstall work as on Debian.
- Fixed the Xray Vision / Trojan / XHTTP camouflage fallback breaking for anything that speaks HTTP/2. Their TLS advertises ALPN `h2,http/1.1`, but the only fallback was the HTTP/1.1 site on 8080, so a browser or active prober that negotiated h2 got a dropped connection instead of the camouflage website. There is now an `alpn: h2` fallback to an h2c listener on 127.0.0.1:8081, and existing camouflage sites gain that listener on the next Xray change.
- Fixed settings stored as `false` being read as `true`: `jq '.x // true'` treats `false` as missing. Xray Vision / Trojan / XHTTP nodes with the camouflage fallback turned off (the `psm node` default) still got a fallback to `127.0.0.1:8080`, where nothing listened, so an active probe saw a TLS server that drops the connection; Xray and mihomo SOCKS nodes with UDP turned off still had UDP enabled.
- Fixed re-running the one-line installer to update PSM aborting with `Your local changes to the following files would be overwritten by merge`. Install and update `chmod +x` every script, and scripts stored as 100644 in the repository then count as modified, so any upstream change to one of them made `git pull` refuse. File modes are no longer treated as local changes (`core.fileMode false`). Real local edits are saved to `~/psm-local-changes-<time>.patch` before being reverted, in both the installer and the menu update (which used to discard them silently), and a checkout that cannot fast-forward is reset to the remote branch.
- Added Alpine Linux (OpenRC) as a first-class target. The service layer now detects the init system from systemd's own `/run/systemd/system` marker and drives either systemd or OpenRC with correct exit codes; PSM-written daemons (Xray, sing-box, mihomo, Hysteria2, realm, the Telegram bot, the VPNGate tunnel, ss-rust) get OpenRC scripts with supervise-daemon, per-service logs in `/var/log/psm/`, and a 1048576 file-descriptor limit. Every systemd timer (traffic accounting, rule-set updates, Reality watchdog, health report, VPNGate watchdog) has a cron equivalent, and the traffic shutdown checkpoint moves to `/etc/local.d`. The installer adds the GNU userland PSM relies on (busybox `date` cannot do `now +1 month`, busybox has no `ss`, no `mktemp --suffix`, no zoneinfo) and swaps busybox crond, which silently ignores `/etc/cron.d`, for cronie. Certificate renewals reload nginx through `rc-service`, iptables rules are saved where Alpine's iptables service restores them, and the fail2ban SSH jail reads `/var/log/messages` when there is no auth.log.
- `bootstrap.sh` starts with a POSIX-sh shim, so `wget -qO- https://psm.jinqians.com | sh` works on a bare Alpine: it installs bash with apk and re-runs itself under bash, reading the keyboard from `/dev/tty`.
- sing-box installs the `-musl` release on musl systems; the plain build is glibc-linked and cannot start there, while OpenRC would still report it as started.
- `svc_is_active` no longer trusts `rc-service status` alone: supervise-daemon keeps saying "started" for as long as it is retrying a child that dies on start (measured: more than 20 seconds), so the check also requires a live child process.
- ss-rust installs natively on OpenRC (official musl build, the same `config.json` layout the upstream script writes, an OpenRC service). Standalone Snell is refused on musl with a pointer to the sing-box / mihomo Snell nodes: the official snell-server does not start there, and gcompat does not help.
- Fixed `uninstall.sh` and `realm_install` aborting on OpenRC (`systemctl daemon-reload` under `set -e`), and added the missing realm removal to `uninstall.sh`. Fixed `install.sh` and the menu exiting when there is no terminal: a bare `clear` fails without `TERM` and `set -e` ended the script.

- Every generated configuration is now checked against the real cores (`tests/core-validate.sh`: Xray v26.3.27 and v26.9.9, sing-box 1.14.0, mihomo v1.19.30; cores that are not installed are skipped). The first run found three Xray configurations that the project had been generating and Xray rejects outright:
  - XHTTP `h2` mode used the HTTP transport, which Xray has removed. It is now carried by XHTTP `stream-one` over HTTP/2; stored nodes migrate on the next apply, clients need the new link.
  - mKCP `seed`/`header` changed shape twice across Xray versions, and no single form is accepted by both v26.3.27 and v26.9.9. PSM now asks the installed Xray which form it takes and generates that one; installing or switching the Xray core rebuilds all inbounds from the node stores instead of replacing a no-longer-valid config with an empty skeleton.
  - REALITY over WebSocket or HTTP/2 was offered but Xray only accepts RAW, XHTTP and gRPC under REALITY. The choice is now XHTTP or gRPC; stored ws/h2 values fall back to XHTTP.
- Fixed adding or editing any XHTTP node silently removing every VMess inbound from `config.json`: the cleanup matched on `network` only and VMess uses WebSocket too.
- Fixed `psm node`: `--kcp-seed`, `--kcp-header` and `--reality-transport` were stored under hyphenated keys and ignored; `psm node export` produced wrong links for XHTTP h2, HTTPUpgrade, mKCP and REALITY+gRPC (it now shares the menu's link builder); REALITY+gRPC links carried a leading slash in `serviceName` that the server strips.
- `tests/config-regression.sh` no longer depends on whether sing-box or Xray is installed on the machine running it.
- Fixed sing-box and mihomo VLESS nodes added with a custom `--tag` being appended a second time on the next change (sing-box then refuses the whole config with `duplicate inbound tag`): the cleanup only matched the `sb-vless-` / `mh-vless-` prefix. It now also removes every non-REALITY VLESS inbound, which is exactly what that module owns.
- Fixed a burst of node changes leaving a core stopped on systemd: five restarts within ten seconds trip the default start limit (`start-limit-hit`), after which every further restart failed until someone ran `systemctl reset-failed`. Restarts now reset the counter first.
- Fixed mihomo TLS listeners (VLESS, Trojan, VMess, AnyTLS, Hysteria2) not starting when the certificate lives outside `/etc/mihomo` — an Nginx/acme.sh domain certificate or any `--cert-path`. Current mihomo only reads files under its home directory or `SAFE_PATHS`; `mihomo -t` does not check this, so the config validated and the listener was then silently missing. With VLESS Encryption on, mihomo's error path dereferences a nil pointer instead, and the panic took every listener down with it. PSM now keeps `SAFE_PATHS` in `/etc/mihomo/psm.env` in step with the certificate directories the config references, and both the systemd unit and the OpenRC script load it; existing installs are migrated on the next change.
- Fixed Xray SS2022 nodes with a custom `--tag` being appended a second time on the next change (Xray then refuses to start with `existing tag found`), and a Vision change deleting every Xray Trojan inbound: Trojan is TLS over TCP too, and the Vision cleanup did not look at the protocol.
- Hysteria2 obfuscation passwords must be at least 4 characters. Xray's Salamander refuses a shorter PSK and then does not start at all, taking every other inbound down with it; the same minimum now applies to the interactive prompts and `psm node` for all three cores.

- Added a Hysteria2 inbound for Xray (Xray v26.3.27+), so an Xray-only server can serve Hy2 without the standalone binary. Self-signed nodes put the certificate's SHA-256 into the link as `pinSHA256`.
- Added Gecko QUIC obfuscation for Hysteria2 on sing-box (1.14+), mihomo (1.19.26+) and Xray (expressed as Salamander with `packetSize`), next to Salamander.
- Added VLESS Encryption (post-quantum ML-KEM-768 + X25519 key exchange) for Xray REALITY / Vision / XHTTP nodes and mihomo VLESS nodes, interactively or with `--vless-enc x25519|mlkem768`. X25519 authentication is the default because the ML-KEM-768 client key is about 1.6 KB. Enabled nodes drop `fallbacks`, which Xray refuses next to `decryption` even as an empty array. Verified end to end: a real client reaches the internet through the real server, and a client holding the wrong key does not.
- Added ShadowTLS v3 for mihomo SS2022 and Snell listeners (`--shadow-tls-sni`), with Clash and Surge snippets instead of a share link. The handshake target gets the same shared-CDN-frontend check as a REALITY dest, since ShadowTLS relays unauthenticated connections to it. Verified end to end with the exact client snippet PSM prints.

- Added subscribed rule sets for sing-box and mihomo (issue #3): paste a community rule-list URL, pick an exit, and the traffic it describes leaves through that exit. Both cores use their native path, so refreshing never restarts anything — mihomo gets a `rule-providers` entry it refreshes on its own interval, and sing-box gets a local `source` rule set, which it reloads by itself when the file changes (1.10+). Xray has no rule-set concept, so its rules are inlined into `config.json` instead, which is also the only core that has to be restarted when a list changes.
- Added `manager.sh --ruleset-update` and a daily timer that keeps the lists current. The refresh cost differs per core and the timer respects that: mihomo needs nothing (the core refreshes its own provider), sing-box only needs its rule-set file rewritten, and Xray is rebuilt and restarted — but only when the content actually changed and Xray really has that rule set bound, so a no-op fetch never drops anyone's connections. The timer carries a randomized delay of up to an hour, because every install pulling the same community repository at the same second is not a nice thing to do.
- Rule lists are parsed once in a shared layer rather than trusted blindly, and the preflight report is shown before anything is applied. Client-only types (`PROCESS-NAME`, `USER-AGENT`, …) and `IP-ASN`, which only mihomo understands, are counted and reported as dropped instead of being silently skipped — a rule set that half-applies is the worst kind to debug. Malformed entries are filtered out too, since one bad line makes a core reject the whole config.
- Both generated forms deliberately split domains and IPs into separate rules. In sing-box a rule's fields are ANDed, so `domain` and `ip_cidr` written into one headless rule would mean "domain AND IP", which never matches; `domain`, `domain_suffix` and `domain_keyword` share one matcher and are ORed, so those do belong together. The same trap applies to Xray and is what the second step has to get right.
- Rule sets are capped at 5000 rules, and the reason is stated in the UI: a full routing table belongs on the client, where domestic traffic is decided before it ever crosses to the VPS, and mihomo matches `classical` rules linearly, which costs CPU under load. Server-side rule sets are for choosing the exit — OpenAI, Netflix and the like.
- Updating compares the rule count against the stored one first and refuses a change that shrinks by more than half or grows more than fivefold. Injecting a third-party URL into routing hands the "which traffic leaves where" decision to whoever maintains that repository, so a wild swing is treated as a signal to stop and ask rather than something to apply. Only https URLs are accepted.

- Fixed Xray and sing-box installs failing outright: the download URL came out as `.../download/[step] Fetching the latest version...\nv26.3.27/Xray-linux-64.zip`. The version resolvers return the tag on stdout while also reporting progress with `log_step`, and `log_info`/`log_warn`/`log_step`/`log_ok` all wrote to stdout, so `tag=$(_xray_resolve_tag ...)` swallowed the log line along with the tag. All log helpers now write to stderr, the way `log_error` already did — logs are diagnostics, not return values, so this removes the whole class of bug rather than patching the two call sites. mihomo was never affected: it resolves its tag inline instead of through a command substitution.
- Fixed the Telegram bind token being generated empty, which silently disabled the check it was supposed to enforce. `tr -dc 'A-Za-z0-9' < /dev/urandom` exits with "Illegal byte sequence" in a UTF-8 locale unless `LC_ALL=C` is set, and the generator was missing it, so `/token <port>` could store an empty token — while the bind path treats an empty stored token as "no token required" and lets anyone bind to that port. The generator now reuses the hardened `rand_str`, an empty token is refused instead of stored, and the failure is reported rather than passing silently.
- Hardened `rand_str`, which produces every password, PSK and camouflage path in the project: it now verifies it actually got the requested length, falls back to `od` plus bash string operations (deliberately not `tr`, which is what failed above), and errors out instead of returning an empty string, so a node can never be created with an empty password.
- Fixed two `set -o pipefail` hazards where `head` exiting early leaves the upstream process with SIGPIPE and the pipeline returning 141: the Telegram token generator (measured non-zero 20 times out of 20, which aborted the bot daemon through a bare assignment under `set -e`) and the nginx dynamic-module lookup.
- Added a VPNGate residential-IP exit, shared by Xray, sing-box, and mihomo. It fetches the public VPNGate server list, classifies each candidate IP through ip-api's batch endpoint, and keeps the ones that are actually home broadband — datacenter nodes and VPNGate's own relays at `219.100.37.x` are demoted, because those are hosted, widely known, and already blocked by the services people want to unlock. The `proxy` flag is deliberately not used for that verdict (nearly every VPNGate node is in public proxy databases, so it would reject the entire list); it is shown as a risk hint instead. Candidates are tried in order until one connects and its exit IP can actually be probed. The exit country is always chosen by the operator from a numbered list of the countries that actually have nodes (with their node counts) rather than defaulted to, since the exit country is what decides which catalogue a streaming service shows.
- The residential tunnel is an openvpn client on its own `psmvg0` device with `--route-nopull`, writing only into a dedicated routing table: the main table is never touched, so SSH and every existing node keep using the datacenter link. Cores reach it by marking connections (`sockopt.mark` on Xray, `routing_mark` on sing-box, `routing-mark` on mihomo) — not by binding to the interface, which needs `CAP_NET_RAW` that none of the three systemd units carry, while `SO_MARK` only needs the `CAP_NET_ADMIN` they already have. Because the outbound refers to a mark rather than a node, rotating to a different residential IP never rewrites any core config. When the tunnel is down the table keeps only a blackhole route, so matched traffic fails closed instead of leaking back out through the datacenter IP.
- Failover is automatic and stays inside the chosen country. VPNGate nodes are volunteers' own machines that can be powered off, renumbered, or unplugged at any moment, so a watchdog probes the tunnel every two minutes and, after two consecutive failures, moves to another node **in the same country** — drifting to a different country would silently change which regional catalogue the unlock rules reach. The dead node is put on a six-hour cooldown so rotation does not keep landing back on it, and when every same-country candidate is exhausted the list for that country is refetched before giving up. OpenVPN is also given `ping`/`ping-restart`, because a peer that loses power never sends a FIN and would otherwise leave the tunnel hanging.
- VPNGate nodes still run AES-128-CBC with SHA-1 certificates, which OpenSSL 3 refuses by default, so generated configs relax `tls-cipher` and `data-ciphers` — but only on OpenVPN 2.5+, since those options do not exist on the 2.4 that ships with Debian 10 / Ubuntu 20.04. A first failure that looks like a legacy-algorithm rejection is retried once with `providers legacy default`. Compression directives from the official config are kept as-is: they describe the framing the far end expects, so dropping them breaks the tunnel.

- Removed the shipped Reality camouflage defaults (`www.cloudflare.com` for Xray/sing-box/mihomo Reality, `www.microsoft.com` for XHTTP reality-layer) on every server-side path, interactive and `psm node` alike. Cloudflare's own domain is the exact case the Xray docs warn about, and any hardcoded well-known domain can move behind a CDN later without the script noticing — while everyone sharing one default is itself a fingerprint. Xray now defaults to mapping-engine discovery, which finds same-ASN targets and so naturally avoids both CDN frontends and large content-distribution sources, matching upstream's "steal a certificate from your own ASN" best practice. sing-box and mihomo have no discovery path, so they require an explicit target instead. The non-interactive CLI fails with a clear message rather than silently building a node on a borrowed default. Client-side outbound SNI prompts are unchanged — they are not affected by this.

- Hardened the Nginx 443 SNI multiplexer against being used as a free relay. Unknown SNI used to fall through to the Reality inbound, and REALITY forwards every connection that fails authentication straight to its `dest` — so anyone who could reach port 443 made the server pay for a round trip to `dest`. When `dest` sits on a multi-tenant CDN frontend, an attacker only had to put an arbitrary CDN-hosted hostname in the ClientHello to get a free tunnel into that entire CDN. Unknown SNI is now dropped without opening any upstream connection, and existing installs are migrated on the next Nginx sync. Mounted nodes are unaffected: each one already has an explicit SNI entry, and a prober using the node's real serverName still gets the full genuine REALITY fallback.
- Added multi-tenant CDN frontend detection for Reality camouflage targets across Xray, sing-box, and mihomo. The check probes the target's IP with unrelated SNIs and flags it when a valid certificate comes back for a hostname the target does not own — testing the property that actually matters (does this IP serve arbitrary tenants?) rather than tracking CDN IP ranges or ASNs. It warns rather than blocks, since the verdict can misfire and the risk is the operator's to weigh.
- Added REALITY fallback rate limiting (`limitFallback*` on Xray, `limit-fallback-*` on mihomo) for nodes whose target was flagged as a shared CDN frontend. It only touches connections that fail REALITY authentication; authenticated clients are unaffected. This is a backstop, not a fix: both parameters are per-connection, so an abuser who reconnects in a loop bypasses them. sing-box has no equivalent option, so nodes on it are warned when a flagged target is combined with direct-listen mode, which leaves no protection layer at all.
- Documented the CDN-fronted `dest` problem in all four READMEs, which so far only existed in this changelog and in code comments. There is now a FAQ entry explaining why REALITY's unconditional fallback turns a CDN-fronted `dest` into a free port forwarder, what the three layers of defence are, and where they do not hold (per-connection `limitFallback`, and sing-box direct-listen having no backstop at all). The 443 port-reuse diagram was also stale: it still showed unmatched SNI falling through to the decoy site, which stopped being true when unknown SNI became a blackhole.

- Added a Xray release channel choice at install/upgrade time (stable or preview). XTLS has marked every release since v26.3.27 as a pre-release, so the stable channel can lag months behind; preview installs the newest build. Stable stays the default and non-interactive runs always use it.
- Added a per-node `minClientVer` setting for Xray Reality nodes. Xray v26.4.13 and newer default it to 26.3.27, which makes the server refuse clients running an older core — including the cores bundled in many phone apps. The installer now warns about this when the preview channel is chosen, and the Reality menu lets you relax the threshold per node. Leaving it unset writes no field at all, so older Xray builds are unaffected.
- Fixed the Xray installer falling back to `v24.9.30`, a release from 2024, and parameterised the fallback message so the version appears in one place only.
- Replaced `www.apple.com` with a neutral placeholder in the twelve Reality/XHTTP camouflage SNI and target prompts across all four languages. Xray now warns that targets containing `apple`, `icloud`, or `microsoft` (and `.cn`/`.ru`/`.ir` suffixes) raise the risk of the server IP being blocked, so the prompts no longer suggest one. The watchdog advice that names these domains as ones to avoid is unchanged.
- Bumped the mihomo fallback version to the current stable and parameterised its message. No mihomo compatibility changes were needed: all five listener types validate cleanly on both v1.19.27 and v1.19.30.
- Fixed the sing-box installer falling back to `v1.14.0`, a tag that does not exist upstream: when the GitHub API is unreachable the fallback is now the latest real stable tag, so the download no longer 404s.
- Added a sing-box release channel choice at install/upgrade time (stable or preview). The preview channel installs the latest beta/rc, which is currently the only way to reach protocols that have not been stabilised upstream yet, such as the Snell inbound (requires 1.14+, still in beta). Stable remains the default and non-interactive runs always use it.
- Added a downgrade guard that refuses to silently install a sing-box build without Snell support while Snell nodes are configured, which would otherwise leave the config failing validation and the service unable to start.
- Fixed the sing-box Snell v5 inbound writing `obfs_host`, a field the Snell inbound does not accept (it belongs to the outbound). sing-box rejected the whole config with `unknown field "obfs_host"`, so obfuscated v5 nodes could never be added. The value is still stored and used for the Surge/Clash client exports.
- Switched remote rule-set downloads to `http_client` on sing-box 1.14+, where the previous `download_detour` is deprecated (removed in 1.16). Installs older than 1.14 keep `download_detour`, since they do not recognise the new field.
- Fixed dual-stack WARP egress verification showing only the IPv6 exit IP: probing is now done per address family via IP-literal endpoints (IPv4 through 1.1.1.1, IPv6 through 2606:4700:4700::1111), so a dual-stack egress reports both its IPv4 and IPv6 WARP exit IPs.
- Added WARP exit-IP verification to sing-box and mihomo, matching Xray: after setup the real egress IP and warp status are probed and shown per family, and a new "Show real WARP exit IP" menu item allows re-checking at any time.
- mihomo WARP setup now registers a WARP identity when missing and supports choosing the egress address family (IPv4 / IPv6 / dual-stack), including an IPv6 tunnel address.
- Extended Nginx 443 SNI multiplexing to sing-box and mihomo Reality/AnyTLS nodes, letting them share the public 443 port alongside Xray nodes.
- Extended per-node traffic metering and quota enforcement (with Telegram quota warnings and automatic pause) to sing-box and mihomo nodes.
- Added a core-services section to the Telegram daily health report covering every proxy core and Nginx.
- Added `psm doctor` with human-readable and stable JSON reports for system, dependency, configuration, core-service, disk, and certificate health.
- Added a non-interactive `psm node` CLI covering 14 Xray/sing-box/mihomo node types with CRUD, export, JSON input/output, default credential redaction, locking, port checks, and transactional rollback.
- Expanded Chinese and English README files with clearer positioning, target users, reasons to choose PSM, uninstall behavior, system write paths, FAQ, and project resource links.
- Added GitHub Issue templates for installation failures, bug reports, and feature requests to make user feedback easier to triage.
- Added `CHANGELOG.md` to track user-visible changes by commit/date until formal versioned releases are introduced.

## 2026-07-03

### Fix uninstall cleanup and reinstall detection

- Fixed incomplete uninstall cleanup so PSM-owned cron entries, systemd units, firewall/Fail2ban wiring, shortcut commands, and the `/opt/psm` install directory can be removed cleanly.
- Added cleanup coverage for additional PSM-managed components such as Snell, ss-rust, and PSM-managed Docker Compose apps.
- Added bootstrap repair detection for stale partial installs, so rerunning the one-line installer can rerun setup when `/opt/psm` exists but the install is incomplete.

## 2026-07-02

### Fix nginx/fail2ban/xray cross-distro bugs and cert rate-limit handling

- Improved cross-distro compatibility across Nginx, Fail2ban, Xray, Docker, system tuning, traffic accounting, and related modules.
- Hardened certificate handling to reduce accidental Let's Encrypt rate-limit risk during acme.sh removal and reinstall flows.
- Improved package/repository handling for RHEL-family systems, including EPEL-dependent dependencies.
- Fixed multiple service-management and config-generation edge cases across proxy, security, backup, and routing modules.

### Initial commit: JQ's Proxy Stack Manager

- Added the initial PSM codebase with one-line install, interactive `psm` menu, update, uninstall, and shared utility framework.
- Added multi-protocol management for Xray Reality / Vision / XHTTP / SS2022, Hysteria2, Snell, and shadowsocks-rust.
- Added Nginx SNI routing, SSL certificate management, Cloudflare DNS/DDNS/Tunnel/Access, Docker app management, traffic accounting, expiry management, Telegram Bot, backup/restore, and security hardening.
- Added Chinese and English README files, templates, and initial project structure.
